import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capacitotest.com',
  appName: 'capaciorTest',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
